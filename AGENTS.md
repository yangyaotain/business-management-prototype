# 业务管理系统静态原型项目约定

本项目是“业务管理系统”静态 HTML 高保真原型，视觉与框架参考
`E:\Cursor\smart-query-prototype`，但业务内容以本项目 `materials/` 中的需求清单和
`docs/需求与原型框架梳理.md` 为准。

## 当前范围

- 当前已确认并完成设计的页面：登录、工作台、人效看板、运营看板、客户评价、
  专项工作、专项结果看板、绩效看板、用户管理、角色管理。
- 工作台已按最新归档需求的 `业务交付指标` 明细表实现 58 项指标：
  - 客户赋能 27 项；
  - 运营效能 26 项；
  - 能力建设 5 项。
- 工作台角色为项目经理、业务组长、质量审核、部门负责人，按责任人列匹配指标；
  指标卡支持名称查询及正常/异常状态筛选，并在角色、周期、工作方向、名称和状态
  联合过滤后按每页 9 项分页。
- 经营管理中的报表报告页面当前只保留统一框架和功能占位，后续确认后设计。
- 人效看板按部门、业务组、个人三级下钻，当前只展示人均产值、人均产能、人均时效和
  人均净利润四项指标；部门负责人查看全部门，业务组长查看本组，普通组员仅查看本人。
- 运营看板按部门、业务组、个人三级同页下钻，支持代理、造价、通用业务类型筛选，
  展示本年实际值与全年目标或年度控制阈值；部门负责人查看全部门，业务组长查看本组，
  普通组员仅查看本人。
- 系统管理当前只包含用户管理和角色管理；角色管理只配置功能权限，不扩展数据权限。
- 客户管理、商机管理、销售管理当前不在原型范围内，不增加菜单或隐藏入口。
- 绩效模块定位为“绩效看板”，不扩展为独立、复杂的绩效管理系统。
- 绩效看板以部门和业务组的实际值、目标值、挑战值、达成率和差距为主，
  不增加个人评分、排名、权重、综合得分、考核审批或薪酬计算。

## 目录约定

- `index.html`：项目入口，跳转登录页面。
- `pages/`：原型页面。
- `assets/css/`：公共主题、布局、组件及页面样式。
- `assets/js/`：公共菜单、公共交互及页面脚本。
- `materials/`：用户提供的原始需求文件，只归档，不修改。
- `docs/`：需求判断、菜单结构、页面边界和后续设计记录。

## 实现规则

- 除独立登录页外，所有业务与管理页面必须复用 `assets/js/navigation.js` 中的公共菜单，
  不在页面内独立维护菜单。
- 所有页面优先复用 `theme.css`、`layout.css` 和已有公共组件。
- 全局字体按统一语义梯度设计：页面标题 22–23px，弹窗和抽屉标题 18px，
  面板标题 15–16px，区块标题和重点标签 14px，正文、按钮、输入控件及主要表格内容
  13px，表头、状态标签、辅助说明及筛选标签 12px，图表刻度、单位和弱化元信息 11px。
  8–10px 仅允许用于未读数字角标、装饰文字或按比例缩放的文档预览，不得用于需要阅读、
  点击或判断状态的语义文字；新增页面必须优先使用 `theme.css` 的字体变量。
- 功能按钮统一使用图标加文字。
- 新增页面先核对同类页面、设计变量和已确认业务范围，不重新创建视觉体系。
- 页面功能只实现用户当前确认的内容，不扩展其他系统管理子模块或数据权限配置。
- 中文文件按 UTF-8 读取和编辑。
- 默认不启动服务、不打开浏览器、不截图；完成后进行 HTML、CSS、JS 和
  `git diff --check` 等静态检查。

## 当前需求事实来源

1. 当前总体范围采用已确认的项目边界；最新 Excel `需求梳理3.0` 中与既有边界冲突的
   其他变化需另行确认，不因本次工作台指标更新自动扩展。
2. 工作台指标采用 2026-07-28 归档版本 Excel 的 `业务交付指标` 明细表，共 58 项；
   完整差异和待实现方案见 `docs/工作台业务交付指标更新分析与设计.md`。
3. `需求梳理2.0` 中的“客户管理（暂缓）”不进入当前菜单。
4. 专项管理已完成创建编辑、处理状态页签、站内消息提醒、进度直接填报、状态调整和
   结果呈现页面，不设置专项填报审批流程。
5. 绩效以业务组层面的实际值、目标值、挑战值、达成率和差距呈现为主。
